// @bun
var __require=import.meta.require;import"@discordjs/rest";import{ActivityType,Client,Collection,Events,GatewayIntentBits as Intents,Partials}from"discord.js";import"discord-api-types/v10";import{readdirSync}from"fs";import{join}from"path";import{setInterval}from"timers";import Utils from"./class/utils.js";import Model from"./class/mongoose.js";var __dirname2=import.meta.dirname,client=new Client({intents:[Intents.GuildExpressions,Intents.Guilds,Intents.GuildInvites,Intents.GuildMembers,Intents.GuildMessages,Intents.GuildMessageReactions,Intents.GuildPresences,Intents.GuildWebhooks,Intents.MessageContent],partials:[Partials.Message,Partials.Channel,Partials.Reaction]});client.commands=new Collection;client.db={};client.utils=new Utils;var body=[],loadCommands=async(type,push)=>{let files=readdirSync(join(__dirname2,`./${type}`)).filter((file)=>file.endsWith(".js"));for(let file of files){let command=await import(`./${type}/${file}`),name=file.slice(0,-3);if(client.commands.set(name,command),push){let info=command.metadata.getInfo();body.push(info)}}},loadEvents=async()=>{let files=readdirSync(join(__dirname2,"./events")).filter((file)=>file.endsWith(".js"));for(let file of files){let{default:event}=await import(`./events/${file}`),eventName=file.slice(0,-3);client.on(eventName,event)}},loadSchemas=async()=>{let files=readdirSync(join(__dirname2,"./schema")).filter((file)=>file.endsWith(".js"));for(let file of files){let schema=await import(`./schema/${file}`),name=file.slice(0,-3);client.db[name]=new Model(name,schema.default)}},activities=["Catharsis","Leap of Faith","Phantasia","Ruins"],activityIndex=Math.floor(client.utils.getUptimeDate().getTime())%activities.length,updateActivity=(client2)=>{client2.user.setActivity({name:activities[activityIndex],type:ActivityType.Playing}),activityIndex=(activityIndex+1)%activities.length};client.on(Events.ClientReady,async()=>{await Promise.all([loadCommands("cmd",!0),loadCommands("ctx",!0),loadCommands("cmd/modal",!1),loadEvents(),loadSchemas()]),updateActivity(client),setInterval(updateActivity,900000,client)});client.login(Bun.env?.DISCORD_TOKEN);process.on("uncaughtException",console.error);process.on("unhandledRejection",console.error);export{client};
